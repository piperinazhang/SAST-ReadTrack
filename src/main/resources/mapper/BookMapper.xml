<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sast.sastreadtrack.mapper.BookMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book(user_id, title, author, total_pages, current_page, status)
        VALUES (#{userId}, #{title}, #{author}, #{totalPages}, #{currentPage}, #{status})
    </insert>

    <update id="updateReadProgress">
        UPDATE book
        SET
            current_page = #{currentPage},
            status = #{status}
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="updateStatus">
        UPDATE book
        SET
            status = #{status},
            current_page = #{currentPage}
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM book WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <select id="selectByUserId" resultType="com.sast.sastreadtrack.entity.Book">
        SELECT * FROM book WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="selectByUserIdAndStatus" resultType="com.sast.sastreadtrack.entity.Book">
        SELECT * FROM book WHERE user_id = #{userId} AND status = #{status} ORDER BY created_at DESC
    </select>

    <select id="searchBooks" resultType="com.sast.sastreadtrack.entity.Book">
        SELECT * FROM book
        WHERE user_id = #{userId}
          AND (title LIKE CONCAT('%', #{keyword}, '%') OR author LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY created_at DESC
    </select>

    <select id="selectByIdAndUserId" resultType="com.sast.sastreadtrack.entity.Book">
        SELECT * FROM book WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="getReadingStats" resultType="com.sast.sastreadtrack.entity.Book">
        SELECT *
        FROM book
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

</mapper>
